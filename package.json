{
  "name": "theloop",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "start": "npx serve ."
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
