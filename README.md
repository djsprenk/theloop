# The Loop

Landing page for The Loop — a DJ learning community.

## Local development

Open a terminal and serve the page with:

```bash
npx serve .
```

Then visit `http://localhost:3000`.

## Deploying

Push to `main`. GitHub Pages serves directly from the repo root — no build step needed. This can take a few minutes.